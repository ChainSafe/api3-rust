pub struct DataPoint {
    value: ethereum_types::U256,
    timestamp: u32
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
